import 'package:flutter/material.dart';

class AllDogsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text('All dogs screen')
      ),
    );
  }
}
